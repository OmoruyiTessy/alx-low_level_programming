#include "main.h"
/**
 * _isupper - super upper characters
 * @c: parameters to be checked
 * Return: can be 0 or 1
 */

int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
	return (1);
	}
	return (0);
}
