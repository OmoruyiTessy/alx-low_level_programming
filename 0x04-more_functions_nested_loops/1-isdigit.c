#include "main.h"
/**
 * _isdigit - function to chect for digit
 * @c: parameter to be check
 * Return: it shoud be 1 or 0
 */
int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
	{
	return (1);
	}
	return (0);
}
