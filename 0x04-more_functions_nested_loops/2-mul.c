#include "main.h"
#include <stdio.h>
/**
 * mul - fuction that multiples two integers
 * @a: parameter1
 * @b: parameter2
 * Return: can be 1 or 0
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
